# Cloudflare Pages Deployment

## Project settings

- **Framework preset**: VitePress
- **Root directory**: `www`
- **Build command**: `npm install && npm run build`
- **Build output directory**: `.vitepress/dist`

## Custom domain

Bind the custom domain `llmconn.com` in Cloudflare Pages.
